pyserial>=3.5
loguru>=0.7.0
# Runtime dependency for the `vd` subcommand (XtalX viscosity/density sensor).
# Imported lazily; the rest of the CLI runs without it.
xtalx>=0.6.0
# Runtime dependency for the `hc` subcommand (NI-DAQ voltage acquisition).
# Imported lazily; the rest of the CLI runs without it.
nidaqmx>=0.9.0
